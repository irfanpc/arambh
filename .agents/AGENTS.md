## Deployment Workflow
- **NEVER** push code directly to GitHub automatically.
- After making changes, ask the user to test the changes locally first.
- Only execute git push after the user has explicitly confirmed the local tests were successful and gives permission to deploy.
